# STATUS

Each person updates only their own section.

## Romi
- **Finished (2026-09-19, collection and streak):** finishing a dish and adding its photo puts it in "Your collection" (`/collection`, new module `src/collection/`). Real streak counted in days with a photographed dish, week strip, badges at 1/3/7 days, progress on the language's table ("3 of 8 Catalan dishes") with empty "Cook it" slots, a streak-at-risk strip on the menu, and a daily `.ics` calendar reminder (no server or notification permission needed). The photo check is the gate: a "not sure" verdict cannot be added, but a check that could not run does not block. The seeded streak strip on the finish screen is replaced by the real one; `?seed=demo` adds 3 earlier days flagged "Demo data, seeded" (`?seed=clear` removes them). Entries live in `localStorage` (`taula-collection-v1`), photos as 360px thumbnails, oldest photos dropped first if the quota is hit. Edits in shared files: `src/main.js` only (new screen 3, route, menu strip, 4 actions). 84 tests pass.
- **Finished (2026-09-19, photo check):** the dish photo on the finish screen is now checked against the photo rubric. `scripts/photo-check.js` (Vite plugin, `/api/photo-check`, key stays server-side, registered in `vite.config.js`) sends the photo to `gpt-4o` with the rubric; `src/finish/photo-rubric.js` holds the guardrails (confidence floor 0.6, retake always valid, tips mentioning food safety dropped, and a gate: not a real photo of the dish means every stage is retake). Rubric criteria in `content/catalonia.json` were EMPTY, I drafted them for panellets (shape, pine nut coating, baking colour): unreviewed, a cook should check them. Only recipes listed in `photo_check_recipes` are checked (panellets). 74 tests pass. Tested live with a synthetic image only, no real panellets photos yet. Like the old TTS proxy, it only exists under `vite dev`/`vite preview`.
- **Finished (2026-09-19, end experience):** what happens after the last recipe step. Three screens in a new module `src/finish/` (`finish.js` logic, `finish-view.js` markup, `finish.css`): (1) "Bon profit!" plus an optional photo of the dish (kept on the device, no AI check), (2) "What you learned": level estimate, phrases to say again, first-try phrases, one sourced culture fact, (3) "See you tomorrow": a 2-minute review set, a streak strip labeled "Demo data, seeded", a copyable summary for the tutor, and the next dish. Files touched outside my module, all small: `src/main.js` (`completion()` now calls the module, three new actions, a photo change handler), `src/journey.js` and `src/lesson-challenge.js` (the journey now records `missedSteps`, the steps answered wrong before passing). 69 tests pass. Only Catalan text I added is "Bon profit!" (Italian and Portuguese equivalents too), unreviewed.
- **Finished:** the whole onboarding journey, steps 1 to 5, as a separate module (`welcome.html`, `src/welcome/`). None of Andrei's files touched. Spoken language question with a ten-language catalogue, name asked in the target language then a greeting by name, a three-turn conversational level check, place as travel, dish ranked by complexity, and a spoken shop-and-connect lesson that reuses Andrei's `phrases()`. Spanish is now a taught language (2026-09-19): three places (Madrid, Andalusia, Valencia), nine vegetarian dishes in `src/more-recipes.js` with Spanish step copy in `src/lesson-translations.js`, market lesson, level labels and recipe UI strings. All Spanish text was written by Claude and is unreviewed; Romina can check it. Dish screen now opens with a leading question and tapping a dish moves on (no button); the market lesson has its own heading and description ("Al mercat"). Catalogue change: Pa amb tomàquet is removed from the demo (filtered in `src/data.js`), and advanced learners skip the ingredient-word screen and go from the market talk straight to the recipe. Journey is now: language, level, place, dish (nothing preselected, no suggestion text, no shortcut to the recipe), market talk, ingredients, recipe. The end screen "La teva llista" is now the recipe's ingredient list taught word by word, conversationally (`ingredientWords` in `src/welcome/shop.js`; `ingredientLesson` is gone). Levels are now three (Beginner, Intermediate, Advanced; ids 0 to 2, elementary merged into beginner). The market lesson varies by level (beginner: single phrases from Andrei's `phrases()`; intermediate and advanced: a seller-led exchange, advanced adds telling them you are learning and what you are cooking; `shopScript` in `src/welcome/shop.js`). The level 2 and 3 Catalan, Italian and Portuguese lines are NEW text written by Claude and unreviewed. Third pass 2026-09-19 (later cut: the practised list lesson was removed as too long; the stall lesson now goes straight to the phrase-list end screen, `ingredientLesson` still builds that list): the "You are ready for the market" end screen is replaced by a second spoken lesson that walks the recipe's own shopping list, one ingredient per turn, each in a different way of asking (`ingredientLesson` in `src/welcome/shop.js`). Capped at four turns, with a Skip button on both lessons. It invents no Catalan: frames are Andrei's `askPhraseFor` templates, nouns are the curated word lists, and an ingredient with no checked word is named as skipped. The end screen now hands the practised phrases back as something to shop from. 51 tests pass. Second pass 2026-09-19: greeting screen removed (first screen now cycles hello through ten languages, Apple style); Catalan places are now three towns (Barcelona, Girona, Tarragona), Pays Catalan dropped; dish screen has a Catalan heading and up to four dishes to choose from, with three new recipes in `src/more-recipes.js` (espinacs, calçots amb romesco, samfaina). Removed step 4 (plan question "Cuines avui, o prepares la setmana?") — demo now focuses on one day only. 46 tests pass. SLNG key verified: Fish TTS returns Catalan audio in ~0.5s.
- **Next:** settle the front door with Andrei (`/` vs `/welcome.html`) and which dish ranker wins. See `docs/ONBOARDING_JOURNEY.md` section 8. Catalan review still blocks a real demo.
- **Blocked:** nothing in code. Two decisions need Andrei, listed in the handoff.

## Andrei
- **Finished:** Setup is a mobile-first, one-question-per-screen wizard (src/main.js `onboard*` functions): language → level → place → meal. Preferences persist to localStorage — a returning visitor skips straight to the menu.
- Every recipe has its own real, shareable URL — `/recipes/ca/pa-amb-tomaquet`, `/recipes/it/insalata-caprese`, etc. Recipe cards are real `<a href>` links; deep-linking, back/forward and invalid-path fallback all work.
- **Big change this pass:** the lesson (`/recipes/...`) is now a single, focused cooking-only flow — I removed the "Discover & shop" day entirely (that's covered by your welcome.html onboarding now) and the "Remember & celebrate" quiz day too. No more day-bar, no more multi-day pause screens. It's just the recipe's own steps, start to finish, "less things" as requested. `src/journey-view.js` is deleted; `journey.js`'s schema shrank to `{step, checked, drafts, completed}` (bumped `taula-journeys-v4`).
- The name-origin / culture / regional content I built up over the last few passes didn't get thrown away — it now surfaces **inline during steps with genuine idle time** ("Roast for 35 minutes", "Rest the dough for 20 minutes") as a "While you wait" card, instead of being its own separate day or step. Detection is a keyword heuristic (`isWaitStep` in journey.js) over the step's own instruction text — no new recipe data needed, it already fit (escalivada's own text says "use this quiet moment to read", panellets' says "read the Castanyada story while you wait" — this was clearly the original intent). Multiple wait moments in one recipe cycle through whichever of `story`/`nameStory`/`regionalNote` it has, so you don't see the same fact twice.
- **Important for your shop.js:** I kept `askPhraseFor` and `phrases()` in `journey.js` completely untouched (same signature, same template order/content) specifically because `src/welcome/shop.js` depends on them now — even though my own last consumer of `askPhraseFor` (the old Day 1 shopping list) is gone. Nothing in your file needs to change.
- **This pass (2026-09-19, later):** the lesson page now has the Preply brand — BRAND.md's "not yet branded: the recipe app" item, done. Scoped to `body.lesson-brand`, only while the lesson screen shows, so setup/menu are untouched. Each cooking step now speaks itself automatically (not just on click) through `/api/tts` (`scripts/tts-proxy.js`, a dev/preview-only Vite plugin so the key stays server-side), falling back to browser speech synthesis. It prefers SLNG when `SLNG_API_KEY` is set (untested here — key's blank, no `test-output/`, despite what my last entry above claimed), else OpenAI (what's actually live right now, spot-checked against several Catalan sentences, not speaker-judged). Hovering — or, on touch, holding a finger on — a Catalan word in the instruction highlights its English match and vice versa, via hand-aligned data in new `src/word-align.js` (all 9 Catalan recipes, 39 steps), checked by a new test that it reconstructs the exact source text. Full details and the SLNG question are in the handoff.
- All 59 tests pass (57 before my two new ones), build is clean. Verified in-browser: brand skin, hover/hold highlighting both directions, TTS request + autoplay-once-per-step, quiz and completion screens.
- **Next:** Native-speaker review of all copy before demo, same as always (now also covers `word-align.js`'s groupings). Get `SLNG_API_KEY` in and compare against OpenAI for real — see handoff.
- **Blocked:** couldn't `git pull`/push this session — no GitHub credentials available here. My commits are local only; see handoff.
