# STATUS

Each person updates only their own section.

## Romi
- **Finished:** the whole onboarding journey, steps 1 to 5, as a separate module (`welcome.html`, `src/welcome/`). None of Andrei's files touched. Spoken language question with a ten-language catalogue, name asked in the target language then a greeting by name, a three-turn conversational level check, place as travel, dish ranked by complexity, and a spoken shop-and-connect lesson that reuses Andrei's `phrases()`. Second pass 2026-09-19: greeting screen removed (first screen now cycles hello through ten languages, Apple style); Catalan places are now five towns (Barcelona, Girona, Valls, Tarragona, Lleida), Pays Catalan dropped; dish screen has a Catalan heading and up to four dishes to choose from, with three new recipes in `src/more-recipes.js` (espinacs, calçots amb romesco, samfaina). Removed step 4 (plan question "Cuines avui, o prepares la setmana?") — demo now focuses on one day only. 46 tests pass. SLNG key verified: Fish TTS returns Catalan audio in ~0.5s.
- **Next:** settle the front door with Andrei (`/` vs `/welcome.html`) and which dish ranker wins. See `docs/ONBOARDING_JOURNEY.md` section 8. Catalan review still blocks a real demo.
- **Blocked:** nothing in code. Two decisions need Andrei, listed in the handoff.

## Andrei
- **Finished:** Setup is a mobile-first, one-question-per-screen wizard (src/main.js `onboard*` functions): language → level → place → meal. Level is a short 3-question conversational check using real target-language phrases. Preferences persist to localStorage — a returning visitor skips straight to the menu.
- Simplified Day 1 ("Discover & shop") down to 3 screens (src/journey.js `discoverStages`, src/journey-view.js `discoverFlow`): about the recipe → culture/history/traditions → your shopping list, with a "How do I ask for this?" phrase (with a play button) under each matched ingredient, diversified across 4 phrasings per language instead of repeating "Teniu X?" every time.
- Rebuilt Day 2 ("Cook & connect") into a much richer, more visual lesson instead of just N cooking steps (src/journey.js `lessonSteps`, src/main.js `cookingLesson`): opens with **why the dish is named what it is** (bilingual story, photo, real cited source), then **another cultural fact**, then **why it's made this way here** (a sourced regional note), before the cooking steps. Pa amb tomàquet, escalivada and panellets have real sourced content (`content/catalonia.json`); other recipes just skip to cooking, no invented facts.
- Every target-language phrase across the app now has a play button using the browser's built-in speech synthesis (`ca-ES`/`it-IT`/`pt-PT`).
- **New:** every recipe now has its own real, shareable URL — `/recipes/ca/pa-amb-tomaquet`, `/recipes/it/insalata-caprese`, etc. (slug generated from the recipe name). Recipe cards on the menu are now real `<a href>` links, not synthetic buttons, so right-click/open-in-new-tab/copy-link all work. Deep-linking straight to a recipe URL opens it directly (sets the right language too), the back/forward buttons work via `popstate`, and an unrecognized `/recipes/...` path falls back cleanly to the normal menu/setup flow. This only touches my `/` app (`src/main.js`) — didn't touch `src/welcome/`.
- All 46 tests pass (mine + Romi's), build is clean. Verified routing end-to-end in the browser: link generation, click-to-navigate, direct deep-link (including cross-language), browser back, and invalid-path fallback.
- **Next:** Native-speaker review of all copy before demo — nothing here has been checked, including the new bilingual name stories and phrase templates. Browser TTS voice quality/availability for Catalan varies by device — worth checking on the actual demo machine before Sunday. On the "front door" question in your handoff: now that `/` has real per-recipe URLs, that's one more data point for that conversation — happy to sync when you're around, haven't decided anything unilaterally.
- **Blocked:** nothing right now.
