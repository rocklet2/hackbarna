# STATUS

Each person updates only their own section.

## Romi
- **Finished:** repo created and connected, CLAUDE.md protocol set up, project brief saved to docs/PROJECT_BRIEF.md
- **Decided:** demo is Catalonia / Catalan / panellets; progress design in docs/PROGRESS_DESIGN.md
- **Next:** find a Catalan reviewer, decide roles, then run the first-hour tests (SLNG Catalan, vision, Galtea)
- **Blocked:** nothing

## Andrei
- **Finished:** Turned "Day 1" into a real interactive first step (src/journey.js, src/journey-view.js): recipe story with sourced culture note, three ingredient words practiced as a mini-quiz, shop-conversation phrases in the target language (with a local-language fallback for Barcelona/Girona/Tarragona), a checkable shopping list with a text download, and curated shop recommendations per city (real market for Barcelona, Maps search elsewhere). Days 2 (cook) and 3 (recall quiz) gate on finishing day 1; progress saves to localStorage per recipe+level. Added a bigger recipe catalog (src/more-recipes.js, 12 more dishes). Did a full mobile pass in a real 375px browser and fixed a CSS wrapping bug (src/style.css).
- Rebuilt setup into a mobile-first, one-question-per-screen wizard (src/main.js: `setup()`/`onboard*` functions): language → level → place → meal, each its own screen with a progress bar and back button. Level is no longer just a self-picked label — it's a 3-question conversational check ("Do you already know common kitchen words like 'pa'?" using real target-language content from journey.js's `phrases()`), and the level = however many you answered "yes" to; there's still a "pick my level myself" fallback link. Removed the separate "Where will you be shopping?" step from setup entirely — shopping city is still chosen later, inside the Day 1 journey's location card, so nothing was lost. Preferences (including whether onboarding was completed) now persist to localStorage: a returning visitor skips straight to the menu; using "Edit preferences" replays the wizard pre-filled, defaulting to the quick manual level picker instead of re-running the test. All 9 tests pass, build is clean, walked through the full wizard + remember/edit paths in a real 375px mobile browser.
- **Next:** Native-speaker review of all new Catalan/Italian/Portuguese strings (shop phrases, quiz text, culture notes, the 3 level-test prompts) before demo — nothing here has been checked. Shop recommendations (Mercat de la Boqueria, La Dispensa, A Casa Portuguesa) are real places but stock/hours aren't verified — say so in the pitch. Decide if 12 extra recipes are needed for the demo or just for menu variety. The level-test is self-reported honesty, not graded — worth deciding whether the pitch should call it a "quick check" rather than a "test" to avoid overclaiming assessment.
- **Blocked:** nothing right now — last push went through fine (branch was "up to date with origin/main"). Noticed a harmless Vite build warning about `content/catalonia.json` being imported with inconsistent `type: "json"` attributes across src/data.js and whatever src/talk/ imports it — not something I touched, flagging in case it's worth a quick fix.
