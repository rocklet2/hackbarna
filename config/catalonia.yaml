# Country configuration: Catalonia / Catalan / panellets
# Country is data, not code. A second country is a second file like this one.
#
# REVIEW STATUS: every Catalan string is UNREVIEWED until a Catalan speaker signs off.
# Culture facts are UNVERIFIED until a source is added. Do not demo unreviewed strings.

country: Catalonia
language: ca
language_name: Catalan
source_language: en
review:
  reviewer: null            # TODO: name of Catalan speaker
  reviewed_on: null

dish:
  id: panellets
  name: Panellets
  target_time_minutes: null  # TODO: confirm under 45
  culture_tag: castanyada    # traditionally linked to All Saints' Day, UNVERIFIED

vocabulary:
  # level = first level where the word is taught. All entries need Catalan review.
  - {en: almond,      ca: ametlla,  level: 1, status: unreviewed}
  - {en: sugar,       ca: sucre,    level: 1, status: unreviewed}
  - {en: egg,         ca: ou,       level: 1, status: unreviewed}
  - {en: oven,        ca: forn,     level: 1, status: unreviewed}
  - {en: bowl,        ca: bol,      level: 1, status: unreviewed}
  - {en: pine nuts,   ca: pinyons,  level: 1, status: unreviewed}
  - {en: to mix,      ca: barrejar, level: 1, status: unreviewed}
  # TODO: numbers, tray, ball, coconut, potato, more actions

stages:
  # Each stage: instruction, "what done looks like", optional timer, optional photo check.
  - id: mix_dough
    level_min: 1
    instruction: {en: "Mix the almond flour and sugar.", ca: null}   # TODO Catalan
    done_looks_like: null   # TODO
    timer_seconds: null
    photo_check: null
  - id: shape_balls
    level_min: 1
    instruction: {en: "Roll the dough into small balls.", ca: null}
    done_looks_like: null
    timer_seconds: null
    photo_check: shaping
  - id: coat
    level_min: 1
    instruction: {en: "Press the pine nuts onto the outside.", ca: null}
    done_looks_like: null
    timer_seconds: null
    photo_check: coating
  - id: bake
    level_min: 1
    instruction: {en: "Bake until golden.", ca: null}
    done_looks_like: null
    timer_seconds: null
    photo_check: baking_color
  # TODO: confirm real recipe and stage list with a cook

photo_rubrics:
  # Appearance only. NEVER food safety. Output must include confidence; "retake" is valid.
  shaping:      {criteria: [], verdicts: [good, fixable, retake]}   # TODO
  coating:      {criteria: [], verdicts: [good, fixable, retake]}   # TODO
  baking_color: {criteria: [], verdicts: [good, fixable, retake]}   # TODO

questions:
  # Embedded in stages. type: recognize | recall | produce | culture
  - {id: q1, stage: mix_dough, type: recall, level: 1, prompt_en: "What is 'egg' in Catalan?", answer_ca: ou, status: unreviewed}
  # TODO: add 2-3 total for the demo

culture_facts:
  # Curated, not generated. Each needs a source before it ships.
  - id: cf1
    level_min: 4
    text_en: "Panellets are traditionally eaten around All Saints' Day."
    source: null            # TODO
    verified: false
    followup_question: null # TODO

seeded_history:
  label: "Demo data, seeded"   # must appear in the UI and the pitch
